# Node.js dependencies
node_modules

# Environment files
.env
.env.local
.env.*.local
!.env.example  # Track example files

# Defense-in-depth: never keep nested env files in the repo.
apps/**/.env
apps/**/.env.local
apps/**/.env.*.local

# SSL/TLS Certificates (NEVER commit certificates or private keys)
*.pem
*.key
*.crt
*.csr
*.p12
*.pfx
certs/
ssl/

# Per-workspace lockfiles (root lockfile only)
apps/*/package-lock.json

# Logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
*.log

# Build output
/dist
/build
**/dist/
**/build/
coverage

# Browser Profiling Data
**/profiling-data*.json
reports/*.json
*.har

# Test output
test-results
playwright-report

# macOS metadata
.DS_Store

# Windows metadata
Thumbs.db
ehthumbs.db

# Local backups
/backups

# Local development files
.dev-data/
.sso-test/
scripts/remote-dev.sh
scripts/reset-dev-node-modules.sh
NEXT_STEPS.md
docker-compose.dev.yml
docker-compose.prod.test.yml
docker-entrypoint-dev.sh
Dockerfile.dev
technitium.env
test*.json
